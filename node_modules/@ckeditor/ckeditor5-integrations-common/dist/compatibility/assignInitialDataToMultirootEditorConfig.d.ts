import { EditorRelaxedConfig } from '../types/EditorRelaxedConfig.js';
/**
 * Assigns the `initialData` property to multiroot configuration.
 *
 * @param data The editor data from the bound component property to be assigned.
 * @param config The editor configuration.
 * @returns The editor configuration with assigned `initialData` property.
 */
export declare function assignInitialDataToMultirootEditorConfig(data: Record<string, string> | undefined, config: EditorRelaxedConfig): EditorRelaxedConfig;
