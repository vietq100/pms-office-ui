const rules = {
  noticeType: [{ required: true }],
  feeType: [{ required: true }],
  period: [{ required: true }],
  method: [{ required: true }],
  beforeOn: [{ required: true }]
}

export default rules
