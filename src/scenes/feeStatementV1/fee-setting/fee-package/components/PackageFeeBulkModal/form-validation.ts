export const packageFeeRules = {
  period: [{ required: true }],
  year: [{ required: true }],
  projectId: [{ required: true }],
  fromToDate: [{ required: true }],
  name: [{ required: true }],
  description: [{ required: false }]
}
